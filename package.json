{
  "name": "parque-catalogo-cli",
  "version": "1.0.0",
  "description": "CLI que lee el catálogo de atracciones de un parque, calcula un resumen (con precio promedio y extremos), filtra por categoría, y escribe un reporte JSON.",
  "type": "module",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc",
    "dev": "tsx src/index.ts",
    "start": "node dist/index.js"
  },
  "devDependencies": {
    "typescript": "^5.5.4",
    "tsx": "^4.16.2",
    "@types/node": "^20.14.10"
  }
}
